cmake_minimum_required(VERSION 3.5)
project(carm_api)

# Default to C99
if(NOT CMAKE_C_STANDARD)
  set(CMAKE_C_STANDARD 99)
endif()

# Default to C++14
if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 14)
endif()

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

# find dependencies
find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(geometry_msgs REQUIRED)

find_package(arm_control_sdk REQUIRED)

if(BUILD_TESTING)
  find_package(ament_lint_auto REQUIRED)
  # the following line skips the linter which checks for copyrights
  # uncomment the line when a copyright and license is not present in all source files
  #set(ament_cmake_copyright_FOUND TRUE)
  # the following line skips cpplint (only works in a git repo)
  # uncomment the line when this package is not in a git repo
  #set(ament_cmake_cpplint_FOUND TRUE)
  ament_lint_auto_find_test_dependencies()
endif()

include_directories(
  include
  ${CMAKE_INCLUDE_CURRENT_DIR}
)

add_executable(carm_ros_node src/carm_ros.cpp)
ament_target_dependencies(carm_ros_node rclcpp std_msgs sensor_msgs geometry_msgs)
target_include_directories(carm_ros_node PRIVATE 
  ${CMAKE_CURRENT_SOURCE_DIR}/include/
  ${arm_control_sdk_INCLUDE_DIRS}
)
target_link_libraries(carm_ros_node
  ${arm_control_sdk_LIBRARIES}
)

add_executable(carm_dual_ros_node src/carm_dual_ros.cpp)
ament_target_dependencies(carm_dual_ros_node rclcpp std_msgs sensor_msgs geometry_msgs)
target_include_directories(carm_dual_ros_node PRIVATE
  ${CMAKE_CURRENT_SOURCE_DIR}/include/
  ${arm_control_sdk_INCLUDE_DIRS}
)
target_link_libraries(carm_dual_ros_node
  ${arm_control_sdk_LIBRARIES}
)

install(TARGETS
  carm_ros_node
  carm_dual_ros_node
  DESTINATION lib/${PROJECT_NAME})

ament_package()
